# Without a key, RouteMaster runs deterministic local/mock providers (works fully offline).
# Provide a key to route to real models.
OPENAI_API_KEY=
CHEAP_MODEL=gpt-4o-mini
STRONG_MODEL=gpt-4o
THRESHOLD=0.5
PORT=3000
